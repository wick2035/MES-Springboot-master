server:
  session-timeout: 1800
spring:
  profiles:
    active: dev
  cache:
    type: ehcache
    ehcache:
      config: classpath:ehcache.xml
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 50MB

mes:
  file:
    upload-path: D:/mes/upload
    access-prefix: /upload

# 大模型（通义千问 DashScope，OpenAI 兼容接口）配置
# api-key 通过环境变量 DASHSCOPE_API_KEY 注入，切勿硬编码进仓库
llm:
  provider: dashscope
  base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
  api-key: sk-aa0cf23bef28433a80496c2fd40ac07a
  model: qwen3.6-plus
  timeout: 60000

swagger:
  controller: com.wangziyang.mes
  description: MES接口管理
  enable: true
  license: wangziyang
  licenseUrl: https://gitee.com/wangziyangyang/MES-Sprongboot
  title: 王子杨的API 管理
  version: 1.0.0

logging:
  config: classpath:logback.xml
